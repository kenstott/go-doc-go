You are an expert knowledge engineer creating domain-specific ontologies for document analysis. Generate comprehensive ontology rules for automatically extracting entities and relationships.

## Domain Context

**Domain**: {domain_name}

**Description**: {domain_description}

**Key Concepts**: {key_concepts}

**Important Relationships**: {key_relationships}

**Regulatory/Business Context**: {regulatory_context}

## Document Analysis Data

### Document Types
{document_types}

### Sample Elements (Actual parsed data)
```json
{sample_elements_json}
```

### Element Statistics
- Total elements analyzed: {total_elements}
- Element types: {element_type_distribution}
- Most common names: {top_element_names}
- Most common paths: {top_paths}

### Metadata Patterns
{metadata_analysis}

## Task: Generate Ontology YAML

Create a complete ontology that:

1. **TERMS**: Define core domain entities by analyzing element names, content, and structure
2. **ELEMENT_MAPPINGS**: Create precise extraction rules using:
   - `search_scope: "element_name"` for XML tags, JSON keys, CSV headers
   - `search_scope: "path"` for hierarchical patterns
   - `search_scope: "attributes"` for XML/HTML attributes
   - `search_scope: "all"` for comprehensive search
3. **RELATIONSHIP_RULES**: Map entity connections from document structure
4. **DERIVED_ENTITIES**: Create analytical aggregations if needed

Focus on:
- High-precision rules (minimize false positives)
- Domain expertise application
- Coverage of sample data patterns
- Meaningful analytical relationships

Generate the complete YAML ontology now:

```yaml
domain:
  name: "{domain_name}"
  version: "1.0.0"
  description: "{domain_description}"
  settings:
    default_confidence_threshold: 0.75

# Your generated ontology here...
```