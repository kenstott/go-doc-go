[pytest]
addopts = -s -v
markers =
    unit: Unit tests (fast, isolated, no external dependencies)
    integration: Integration tests (may require external services/binaries)
    performance: Performance benchmark tests
    compatibility: Go/Python compatibility tests
    slow: Slow-running tests (>1 second)
    comprehensive: Comprehensive test coverage (more thorough than unit tests)
